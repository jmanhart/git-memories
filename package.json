{
  "name": "git-memories",
  "version": "1.0.6",
  "description": "A CLI tool to revisit your GitHub contributions on this day throughout the years",
  "scripts": {
    "dev": "./dev.sh",
    "dev:quick": "./dev-quick.sh",
    "build": "npm run build:core && npm run build:web",
    "build:core": "cd packages/core && npm run build",
    "build:web": "cd packages/web && npm run build",
    "start:backend": "cd packages/web/backend && npm start",
    "start:web": "cd packages/web && npm run dev",
    "install:all": "npm install && cd packages/core && npm install && cd ../web && npm install && cd backend && npm install",
    "clean": "rm -rf packages/*/node_modules packages/*/dist node_modules"
  },
  "keywords": [
    "git",
    "github",
    "contributions",
    "cli",
    "terminal",
    "memories"
  ],
  "author": "John Manhart",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ]
}
